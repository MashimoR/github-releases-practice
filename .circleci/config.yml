version: 2.1

jobs:
  sample:
    docker:
      - image: debian:10
    steps:
      - checkout
      - when:
          condition:
            matches:
              value: << pipeline.git.branch >>
              pattern: /^.+$/
          steps:
            - run: echo 'hit!'

      - run:
          name: make directory
          command: |
            echo 'hello'

workflows:
  version: 2
  welcome:
    jobs:
      - sample
